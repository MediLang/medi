[package]
name = "medi_rt"
version = "0.1.4"
edition = "2021"
description = "Medi standard library: real-time streaming, low-latency I/O, and device protocol support"
license = "Apache-2.0"

[lib]
name = "medi_rt"
path = "src/lib.rs"

[dependencies]
