# Benchmarks

This page includes the latest benchmark report generated by the benchmark runner.

To regenerate the report:

```bash
cargo run -p medic_benchmarks --bin benchmark_healthcare_workloads --release
```

--8<-- "technical/healthcare_workloads_latest.md"
